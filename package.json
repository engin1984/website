{
  "name": "web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "rimraf dist && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "bulma": "^1.0.4",
    "rimraf": "^6.1.3",
    "sass": "^1.98.0"
  }
}
